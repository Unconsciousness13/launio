# from django.core.exceptions import ValidationError
#
#
# def validate_only_letters(value):
#     invalid_chars = [ch for ch in value if not ch.isalnum()]
#     if invalid_chars:
#         raise ValidationError(f'Tiene que contener solo letras,pero contiene: {invalid_chars}')
